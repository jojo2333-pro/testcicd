{
  "name": "mini-web",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "build:ci": "turbo run build --filter=...[HEAD]", 
    "clean": "turbo run clean && rimraf node_modules .turbo"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.11.1",
  "devDependencies": {
    "cross-env": "^10.0.0",
    "rimraf": "^6.0.1",
    "turbo": "^2.5.6"
  }
}
