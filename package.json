{
  "name": "mini-web",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "vite",
    "build": "vite build",
    "build:cli": "cross-env BASE_PATH=/${REPO}/ vite build",
    "clean": "rimraf dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.11.1",
  "devDependencies": {
    "cross-env": "^10.0.0",
    "rimraf": "^6.0.1",
    "turbo": "^2.5.6"
  }
}
